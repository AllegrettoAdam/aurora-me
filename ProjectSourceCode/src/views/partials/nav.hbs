<header>
    <nav class="navbar navbar-expand-sm border-bottom">
        <div class="container">
            {{!-- <button class="navbar-toggler ms-auto" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbar-collapse" aria-controls="navbar" aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button> --}}
            <div class="collapse navbar-collapse" id="navbar-collapse">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/finder">Finder</a>
                    </li>
                </ul>
                <div class="nav-item">
                    <a class="nav-link" href="/social">Social</a>
                </div>
            </div>
        </div>
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="/logout">Logout</a>
            </li>
        </ul>
        {{!-- TODO: Figure out how to access user's profile picture --}}
        {{!-- TODO: Do we want (href) references like this or like above? --}}
        <a href="/profile">
        
        {{!--Only displayu7 if thjere is a user--}}
            {{#if user}}
            <img id="profile_picture" class="rounded float-end" src="{{user.profile_pic}}" alt="Profile picture"
                width="60" height="60">
            {{/if}}
        </a>
    </nav>
</header>